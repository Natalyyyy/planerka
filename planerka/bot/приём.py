"""Приём нажатий кнопок «Беру / Подумать / Нет».

Единственное место в боте, которое трогает файл недели — а в нём лежит
недельная работа человека: отметки, план по дням, свои разделы. Такт А
отказывается переписывать файл с чужой разметкой (`runner/takt_a.py`),
такт Б владеет только двумя своими разделами и сохраняет остальное дословно
(`runner/takt_b.py`). Этот модуль держит ту же планку: правится ровно один
символ в одной строке «Банка тем», ничего больше.

## Как находится строка

`отправка.py` кладёт в `callback_data` кнопки хеш от ВСЕЙ строки банка после
чекбокса — тема плюс `· откуда: источник` (`planerka.bot.темы.идентификатор`).
Идентификатор не зависит от текущего маркера строки: строку находим по её
содержимому, какой бы маркер в ней сейчас ни стоял — свежая пустая клетка,
маркер, который бот сам поставил раньше, или маркер, который человек
проставил руками. Это и даёт контракт, зафиксированный при отправке: если
человек поправит текст уже отправленной темы, идентификатор в старой кнопке
перестанет находиться в файле — и нажатие обязано сказать об этом, а не
промолчать (см. `_ОТВЕТЫ["тема_не_найдена"]` ниже).

Строка ищется только внутри раздела «Банк тем», тем же обходом (блоки кода,
вложенные строки, границы разделов), что и `runner.decisions._разобрать`, —
переиспользуются его регулярки (`СТРОКА`, `ЗАГОЛОВОК_БАНКА`,
`ЗАГОЛОВОК_РАЗДЕЛА`, `СОСТОЯНИЕ`), чтобы не завести вторую, слегка другую
трактовку того, что вообще считается строкой банка. Чекбокс с тем же
текстом, случайно написанный человеком в другом разделе, — не банк, и
нажатие туда не полезет: `_строки_банка_с_позициями` физически не увидит
его вне раздела.

## Гонка с тактом Б (и вообще с кем угодно)

Такт Б читает файл, зовёт модель (десятки секунд) и только потом пишет
план — если приём успел записать нажатие между этими двумя точками такта Б,
не глядя на файл ещё раз, такт Б собрал бы итоговый текст из УСТАРЕВШЕГО
чтения и молча стёр бы нажатие. Это чинится на стороне такта Б: он
перечитывает файл заново после ответа модели и собирает результат из
свежего текста, а не из того, что читал перед вызовом.

У приёма — обратная гонка: между тем, как он прочитал файл и решил, какую
строку менять, и тем, как он реально запишет результат, кто угодно (такт Б,
человек руками, другое нажатие) мог успеть переписать файл — например,
дописать план. Если писать поверх старого чтения не глядя, придётся
свежедописанный план. Поэтому прямо перед записью приём перечитывает файл
ещё раз и сравнивает с тем, что читал: расхождение — не пишет вообще,
возвращает статус `файл_изменился` и просит человека нажать ещё раз. Это не
полная защита от гонки (между этой проверкой и самим `os.replace` есть
свой, гораздо более узкий зазор), но она убирает ровно тот сценарий,
который был воспроизведён — не безмолвную потерю чужой записи, а честный
отказ и просьбу повторить.

## Переводы строк — как в файле

Файл читается `runner.файлы.прочесть_как_есть`, не `Path.read_text`:
`\r\n` (Windows-редактор, синхронизация с Windows-машины) должен остаться
`\r\n`, а не схлопнуться в `\n` при чтении — иначе «нажатие меняет ровно
один символ» превращается в «нажатие меняет каждую строку файла».

## Два одинаковых нажатия

Дважды нажатая (или нажатая, потом отменённая другой кнопкой) тема не
дублирует строку — меняется маркер той же самой строки, найденной по тому
же содержимому. Если человек успел отметить тему руками другим маркером —
нажатие всё равно побеждает: решение через бота считается более свежим,
чем то, что стояло в файле на момент нажатия. Но перезапись — не тихая:
подтверждение называет и старое, и новое решение по имени («раньше стояло
«нет», записано «беру»») и просит Телеграм показать это модальным окном
(`show_alert=True`), а не полоской, которая гаснет за пару секунд — именно
для того человека, кто задел кнопку мельком, листая переписку за неделю.
Без этого случайное нажатие по старому сообщению в чате молча
переворачивает обдуманное решение, а подтверждение «записано» выглядит так
же, как если бы это было первое решение по теме. Если в строке уже стоит
именно тот маркер, который просит нажатие, — файл не трогается вовсе:
письмо «уже отмечено» подтверждает нажатие полоской (менять было не на
что, это не отказ и не перезапись), запись не происходит, и это самый
строгий случай инварианта «не больше одного символа» — здесь их меняется
ровно ноль.

## Кнопка подтверждается всегда — и подтверждение не рушит уже сделанную запись

`ответить_на_нажатие` вызывается в конце каждого пути — успешного,
идемпотентного и любого из отказов, но сама она — сеть (Bot API), и сеть
может подвести уже после того, как файл записан (протухший
`callback_query_id`, обрыв, таймаут). Все вызовы идут через `_подтвердить`,
которая гасит ошибки этого конкретного вызова: файл — источник истины,
и то, что не получилось помигать кнопкой у человека, не повод откатывать
уже сделанную запись или ронять вызывающий код исключением. Молчание
осталось бы неотличимым от того, что нажатие потерялось; а незамеченная
не найденная тема — самый опасный случай промолчать: человек увидит, что
кнопка отработала, и подумает, что решение записалось, хотя файл не
тронут.
"""
import re
from pathlib import Path

from .api import ОшибкаБота, ответить_на_нажатие
from .темы import ДЛИНА_ИДЕНТИФИКАТОРА, идентификатор

try:  # planerka.bot.* — вызов из тестов и из пакета planerka
    from ..runner.decisions import ЗАГОЛОВОК_БАНКА, ЗАГОЛОВОК_РАЗДЕЛА, СОСТОЯНИЕ, СТРОКА
    from ..runner.weeks import найти_файл_недели, файлы_недель
    from ..runner.файлы import записать_атомарно, прочесть_как_есть
except ImportError:  # bot.* — вызов как в run.py, планёрка добавляет свой
    # каталог в sys.path напрямую (см. run.py) — planerka тогда не общий
    # предок для bot и runner, а два независимых пакета верхнего уровня.
    from runner.decisions import (  # type: ignore[import-not-found]
        ЗАГОЛОВОК_БАНКА,
        ЗАГОЛОВОК_РАЗДЕЛА,
        СОСТОЯНИЕ,
        СТРОКА,
    )
    from runner.weeks import найти_файл_недели, файлы_недель  # type: ignore[import-not-found]
    from runner.файлы import записать_атомарно, прочесть_как_есть  # type: ignore[import-not-found]

# Код кнопки (см. planerka/bot/отправка.py, _КНОПКИ) → символ маркера,
# который встаёт в чекбокс строки банка.
КОД_К_МАРКЕРУ = {"b": "x", "d": "?", "n": "-"}
ИМЯ_РЕШЕНИЯ = {"x": "беру", "?": "подумать", "-": "нет"}

_ХЕКС_ИДЕНТИФИКАТОРА = re.compile(rf"^[0-9a-f]{{{ДЛИНА_ИДЕНТИФИКАТОРА}}}$")

# Человеку без разницы, какая именно из двух поломок случилась с кнопкой —
# итог один и тот же: открыть файл и отметить тему руками. Различение
# («не тот формат» vs «идентификатор битый») остаётся в возвращаемом
# статусе для кода/логов, а не в тексте, который видит человек.
_ТЕКСТ_ПОВРЕЖДЁННОЙ_КНОПКИ = (
    "Не смогла разобрать эту кнопку — решение не записано. "
    "Откройте файл недели и отметьте тему вручную."
)


class _ОшибкаФормата(ValueError):
    """callback_data не того формата: нет двоеточия, лишние части, неизвестный код кнопки."""


class _ОшибкаИдентификатора(ValueError):
    """Формат в порядке, а часть до двоеточия — не тот хеш, что выпускает
    `planerka.bot.темы.идентификатор`."""


def _разобрать_callback_data(данные: str) -> tuple[str, str]:
    """Возвращает (идентификатор, символ_маркера) или бросает одну из двух
    приватных ошибок формата — разные причины отказа, чтобы вызывающий код
    мог различить их в статусе, даже если человеку текст один и тот же."""
    if not isinstance(данные, str):
        raise _ОшибкаФормата
    части = данные.split(":")
    if len(части) != 2:
        raise _ОшибкаФормата
    ид, код = части
    if код not in КОД_К_МАРКЕРУ:
        raise _ОшибкаФормата
    if not _ХЕКС_ИДЕНТИФИКАТОРА.match(ид):
        raise _ОшибкаИдентификатора
    return ид, КОД_К_МАРКЕРУ[код]


def _строки_банка_с_позициями(текст: str) -> list[dict]:
    """(индекс строки, сырой символ маркера, содержимое) для каждой валидной
    строки раздела «Банк тем», в порядке появления в файле.

    Обход намеренно повторяет `runner.decisions._разобрать`: блоки кода,
    вложенные (с отступом) строки и границы разделов «## ...» — те же
    правила, что решают, какая строка вообще годится в решение человека.
    Не переиспользуется сама `_разобрать` (имя с подчёркиванием — приватная
    функция чужого модуля), но регулярки и правило «что считается строкой
    банка» — общие с ней, импортированы напрямую, а не списаны заново.
    """
    lines = текст.splitlines()

    закрытые_блоки_кода: set[int] = set()
    в_блоке_кода = False
    начало_блока = None
    for i, line in enumerate(lines):
        if line.strip().startswith("```"):
            if not в_блоке_кода:
                начало_блока = i
                в_блоке_кода = True
            else:
                закрытые_блоки_кода.update(range(начало_блока, i + 1))
                в_блоке_кода = False
                начало_блока = None

    в_блоке_кода = False
    в_разделе_банка = False
    найдено: list[dict] = []

    for i, line in enumerate(lines):
        if line.strip().startswith("```"):
            в_блоке_кода = not в_блоке_кода
            continue
        if i in закрытые_блоки_кода:
            continue
        if ЗАГОЛОВОК_РАЗДЕЛА.match(line.strip()):
            в_блоке_кода = False
            в_разделе_банка = bool(ЗАГОЛОВОК_БАНКА.match(line.strip()))
            continue
        if в_блоке_кода or not в_разделе_банка:
            continue
        if line and line[0] in " \t":
            continue  # вложенная строка — не решение верхнего уровня

        м = СТРОКА.match(line.strip())
        if not м:
            continue
        маркер = м[1]
        if маркер.lower() not in СОСТОЯНИЕ:
            continue
        содержимое = м[2].strip()
        if not содержимое:
            continue
        найдено.append({"индекс": i, "маркер": маркер, "содержимое": содержимое})

    return найдено


def _заменить_маркер(строка_с_концом: str, новый_маркер: str) -> str:
    """Меняет ровно один символ — тот, что стоит между ПЕРВОЙ `[` и `]` в
    строке (открывающая скобка чекбокса), — оставляя всё остальное в строке
    (включая перевод строки на конце и любые другие `[...]` в тексте самой
    темы) как есть. Именно поэтому — `index`, а не `rindex`: тема вида
    «Разобрать [пункт 3] договора» несёт свою собственную пару скобок
    ПОСЛЕ чекбокса, и взять последнюю `[` в строке — значит сломать текст
    темы вместо того, чтобы проставить галочку."""
    открывающая = строка_с_концом.index("[")
    return строка_с_концом[: открывающая + 1] + новый_маркер + строка_с_концом[открывающая + 2 :]


_ОТВЕТЫ = {
    "файла_недели_нет": "Файл недели не найден — решение не записано.",
    "банк_не_разобрался": (
        "Файл недели есть, но раздел «Банк тем» в нём пуст или не "
        "разобрался — решение не записано. Откройте файл и проверьте раздел."
    ),
    "тема_не_найдена": (
        "Эта тема в файле недели изменилась или её больше нет — решение "
        "не записано. Откройте файл недели и проверьте вручную."
    ),
    "ошибка_чтения": (
        "Не удалось прочитать файл недели — решение не сохранено, попробуйте ещё раз."
    ),
    "ошибка_записи": (
        "Не удалось записать файл недели (занят или нет прав) — решение "
        "не сохранено, попробуйте ещё раз."
    ),
    "файл_изменился": (
        "Файл недели изменился, пока записывалось решение, — попробуйте "
        "нажать ещё раз."
    ),
}


def _подтвердить(токен: str, id_нажатия: str, текст: str, show_alert: bool = False) -> None:
    """Подтверждает нажатие человеку, но не даёт сети испортить остальное.

    `ответить_на_нажатие` — это реальный сетевой вызов Bot API, и он может
    подвести (протухший `callback_query_id`, обрыв, таймаут) уже ПОСЛЕ
    того, как решение записано в файл. Файл к этому моменту — источник
    истины: то, что не получилось помигать кнопкой у человека, не повод ни
    откатывать уже сделанную запись (откатывать и нечем — файл на диске),
    ни ронять вызывающий код исключением, из-за которого то же самое
    нажатие приедет опросом обновлений снова и снова.
    """
    try:
        ответить_на_нажатие(токен, id_нажатия, текст, show_alert=show_alert)
    except ОшибкаБота:
        pass


def принять_нажатие(токен: str, id_нажатия: str, callback_data: str, notes_root: Path) -> dict:
    """Обрабатывает одно нажатие кнопки и сразу подтверждает его человеку.

    Возвращает словарь `{"статус": ..., "файл": путь_недели | None}` —
    для вызывающего кода (будущий цикл опроса обновлений) и для тестов.
    Статусы: `записано`, `без_изменений` (в строке уже стоял тот же
    маркер — файл не тронут), `тема_не_найдена`, `файл_изменился` (гонка —
    файл поменялся между чтением и записью, ничего не записано),
    `файла_недели_нет`, `банк_не_разобрался` (файл недели есть, но в нём
    нечего разбирать), `формат_кнопки_неверный`, `идентификатор_кнопки_битый`,
    `ошибка_чтения`, `ошибка_записи`.

    Ни один путь не бросает исключение наружу: сеть и файловая система —
    внешний мир, который может подвести в любой момент, а кнопка у
    человека должна получить понятный ответ, а не упасть в трейсбек бота.
    """
    try:
        ид, желаемый_маркер = _разобрать_callback_data(callback_data)
    except _ОшибкаФормата:
        _подтвердить(токен, id_нажатия, _ТЕКСТ_ПОВРЕЖДЁННОЙ_КНОПКИ, show_alert=True)
        return {"статус": "формат_кнопки_неверный", "файл": None}
    except _ОшибкаИдентификатора:
        _подтвердить(токен, id_нажатия, _ТЕКСТ_ПОВРЕЖДЁННОЙ_КНОПКИ, show_alert=True)
        return {"статус": "идентификатор_кнопки_битый", "файл": None}

    путь_недели = найти_файл_недели(notes_root)
    if путь_недели is None:
        if файлы_недель(notes_root):
            # Файл(ы) недели физически есть — «не найден» тут было бы
            # неправдой. Настоящая причина — банк тем пуст или ни одна
            # строка в нём не разобралась (найти_файл_недели пропускает
            # такие файлы, см. runner/weeks.py).
            _подтвердить(токен, id_нажатия, _ОТВЕТЫ["банк_не_разобрался"], show_alert=True)
            return {"статус": "банк_не_разобрался", "файл": None}
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["файла_недели_нет"], show_alert=True)
        return {"статус": "файла_недели_нет", "файл": None}

    try:
        текст = прочесть_как_есть(путь_недели)
    except OSError:
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["ошибка_чтения"], show_alert=True)
        return {"статус": "ошибка_чтения", "файл": путь_недели}

    записи = _строки_банка_с_позициями(текст)
    целевая = next(
        (з for з in записи if идентификатор(з["содержимое"]) == ид), None
    )
    if целевая is None:
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["тема_не_найдена"], show_alert=True)
        return {"статус": "тема_не_найдена", "файл": путь_недели}

    текущий_маркер = целевая["маркер"].lower()
    if текущий_маркер == желаемый_маркер:
        _подтвердить(
            токен, id_нажатия,
            f"Уже отмечено как «{ИМЯ_РЕШЕНИЯ[желаемый_маркер]}» — ничего не меняю.",
        )
        return {"статус": "без_изменений", "файл": путь_недели}

    строки_с_концами = текст.splitlines(keepends=True)
    строки_с_концами[целевая["индекс"]] = _заменить_маркер(
        строки_с_концами[целевая["индекс"]], желаемый_маркер
    )
    новый_текст = "".join(строки_с_концами)

    # Гонка: пока приём искал строку и собирал новый текст, файл мог
    # измениться на диске — такт Б мог дописать план (модель думает
    # десятки секунд), человек мог отредактировать файл руками. Перечи-
    # тываем прямо перед записью и сравниваем с тем, что читали в начале:
    # расхождение — не пишем вообще, файл на диске важнее одного нажатия.
    try:
        свежий_текст = прочесть_как_есть(путь_недели)
    except OSError:
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["ошибка_чтения"], show_alert=True)
        return {"статус": "ошибка_чтения", "файл": путь_недели}

    if свежий_текст != текст:
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["файл_изменился"], show_alert=True)
        return {"статус": "файл_изменился", "файл": путь_недели}

    try:
        записать_атомарно(путь_недели, новый_текст)
    except OSError:
        _подтвердить(токен, id_нажатия, _ОТВЕТЫ["ошибка_записи"], show_alert=True)
        return {"статус": "ошибка_записи", "файл": путь_недели}

    # Прежнее решение называем по имени, только если оно вообще было — то
    # есть в строке до нажатия стоял другой маркер решения (не пустая
    # клетка). Без этого случайное нажатие по старому сообщению в чате
    # молча переворачивает то, что человек уже решил, а подтверждение
    # «записано» выглядит так же, как если бы это было первое решение —
    # разницу узнать неоткуда. Такую перезапись показываем модальным
    # окном (show_alert=True), а не полоской — ради того же человека,
    # который мельком задел старую кнопку.
    прежнее_решение = ИМЯ_РЕШЕНИЯ.get(текущий_маркер)
    if прежнее_решение is not None:
        текст_подтверждения = (
            f"Записано: «{ИМЯ_РЕШЕНИЯ[желаемый_маркер]}». Раньше у этой темы "
            f"стояло «{прежнее_решение}» — если это случайное нажатие, "
            "поправьте в файле."
        )
        show_alert = True
    else:
        текст_подтверждения = f"Записано: «{ИМЯ_РЕШЕНИЯ[желаемый_маркер]}». Файл недели обновлён."
        show_alert = False

    _подтвердить(токен, id_нажатия, текст_подтверждения, show_alert=show_alert)
    return {"статус": "записано", "файл": путь_недели}
